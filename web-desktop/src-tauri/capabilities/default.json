{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main desktop window (least-privilege).",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default", "fs:default", "dialog:default"]
}
