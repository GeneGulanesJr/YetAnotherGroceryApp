# Application icons are generated from a source image, not committed by default.
# Generate them with:  npm run tauri icon ./assets/source-icon.png
# Tauri requires these files before `tauri build` will succeed.
